export const initialLoginState = {
  loggedIn: false,
  loggingIn: false,
  user: {},
};

export const initialSignupState = {
  registered: false,
  lregistering: false,
  user: {},
};

export const initialRedflagState = {
  isFetching: true,
  redflags: [],
};
